---
/**
 * F06 — The Word (testimonials).
 * Data-driven from the Astro Content Collection; typographic quotes only —
 * no emoji stars, mono venue/event attribution.
 */
import { getCollection } from 'astro:content';

const testimonials = await getCollection('testimonials');
---

<section id="word" class="py-24" aria-labelledby="word-title">
  <div class="mx-auto max-w-3xl">
    <p class="eyebrow" id="word-eyebrow">The Word</p>
    <h2 class="text-display text-ink" id="word-title">What they said</h2>

    {
      testimonials.length === 0 ? (
        <p class="mt-12 text-ink-soft">No quotes yet — the mic is listening.</p>
      ) : (
        <div class="mt-12 grid gap-10 md:grid-cols-2">
          {testimonials.map((t) => (
            <blockquote class="border-l-2 border-accent-soft pl-6 italic">
              <p class="text-lg text-ink">"{t.data.quote}"</p>
              <footer class="mt-3 font-sans text-label text-accent-deep">
                — {t.data.name}
                {t.data.venue && <>, {t.data.venue}</>}
                {t.data.eventType && <>, {t.data.eventType}</>}
              </footer>
            </blockquote>
          ))}
        </div>
      )
    }
  </div>
</section>
